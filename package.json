{
  "name": "sixthings",
  "version": "1.0.0",
  "description": "六件事 · Six Things — Ivy Lee 法 Web/PWA 清单",
  "license": "MIT",
  "private": true,
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.cjs",
    "test:db": "node scripts/test-db.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
